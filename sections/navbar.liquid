{% schema %}
  {
    "name": "Menu Bar",
    "settings": [
		{
		   "type": "link_list",
		   "id": "main_nav",
		   "label": "Main navigation"
		}	    
    ]
  }
{% endschema %}


    <!-- Menu -->

    <section class="headers" id="headers-19">
        <div id="navbar" class="ph-15 ph-sm-30 ph-lg-70 fixed-navbar fixed-navbar-notification">
            <div class="row justify-content-center align-items-center justify-content-sm-between h-100">
                <div class="col-auto">
                    <div class="row justify-content-center align-items-center">
                        <div class="col-auto" id="company-logo">
                            <a href="{{ shop.url }}">{{ 'living-streets.svg' | asset_url | img_tag: shop.name }}</a>
                        </div>
                        
                        <div class="col-auto d-none d-lg-block">
                            <nav class="navbar navbar-left">
	                            {% for link in linklists[section.settings.main_nav].links %}
	                            <a class="{% if link.active %}active {% endif %}" href="{{ link.url }}">{{ link.title }}</a>
	                            {% endfor %}
	                        </nav>       
                        </div>
                    </div>
                </div>

                <div class="col-auto d-none d-lg-block">
                    <nav class="navbar">                       
                        <a href="{{ shop.url }}/account" class="cart">{{ 'user-round-50-thick.svg' | asset_url | img_tag: '' }}</a>
                        <a href="{{ shop.url }}/cart" class="cart">{{ 'cart.svg' | asset_url | img_tag: '' }}</a>
                    </nav>
                </div>

                <div class="col-auto d-block d-lg-none">
                    <nav class="navbar"><a class="hamburger" id="responsive">{{ 'hamburger.svg' | asset_url | img_tag: '' }}</a></nav>
                </div>
            </div>
        </div>
    </section><!-- Mobile menu -->

    <div class="row justify-content-center align-items-center d-lg-none">
        <div id="mobile-navbar" class="mobile-navbar">

            {% for link in section.settings.main_nav.links %}
            <p><a class="{% if link.active %}active {% endif %}" href="{{ link.url }}">{{ link.title }}</a></p>
            {% endfor %}

            <p><a href="">{{ 'user-round-50-thick.svg' | asset_url | img_tag: '' }}</a></p>
            <p><a href="{{ shop.url }}/cartt">{{ 'cart.svg' | asset_url | img_tag: '' }}</a></p>
        </div>
    </div><!-- Menu -->
    
    